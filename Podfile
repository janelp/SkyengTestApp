platform :ios, '12.0'

use_frameworks!

  def shared_pods
      pod 'Alamofire', '~> 5.2'
      pod 'AlamofireImage'
      pod 'SwiftyBeaver'
  end

target 'SkyengTestApp' do
    shared_pods
end

target 'SkyengTestAppTests' do
  shared_pods
end
